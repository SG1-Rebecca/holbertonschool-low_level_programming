#include "lists.h"
#include <stddef.h>
#include <string.h>
/**
 * add_node - 
 * @head:
 * @str: string to be duplicated
 * 
 * Return: 
 */
list_t *add_node(list_t **head, const char *str)
{
    list_t *new_node;
    char *duplicated_str;

    if (head == NULL || str == NULL)
    {
        return (NULL)
    }

 return (new_node);   
}